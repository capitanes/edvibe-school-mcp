{
  "name": "edvibe-school-mcp",
  "version": "0.0.0-experimental",
  "private": true,
  "description": "Stateless MCP server for the Edvibe School API. Experimental/unofficial pilot — not yet transferred to Edvibe.",
  "license": "UNLICENSED",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "manifest": "node scripts/generate-manifest.js",
    "normalize": "node scripts/normalize.js",
    "build:contract": "node scripts/generate-manifest.js && node scripts/normalize.js",
    "validate": "node scripts/validate.js",
    "ci": "node scripts/validate.js"
  }
}
